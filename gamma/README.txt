Activate:
$ source .bash_init_conda
$ conda fermi activate
$ conda activate fermi

$ gtvcut
Input FITS file[] L200413231947BB723D3618_PH00.fits
Extension name[EVENTS]

Parallel:

parallel --eta -j 0 --load 80% --noswap -k < geek 
